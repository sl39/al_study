from collections import Counter

N = int(input())
ls = list(map(int,input().split()))
ct = Counter(ls)
stack = []
result =[-1] *N
for i in range(N):
    while stack and ct[ls[stack[-1]]] < ct[ls[i]]:
        result[stack.pop()] = ls[i]
    stack.append(i)
print(*result)
